# Momentum Smart Contracts
## Initialize
Make sure to have `node` >= 18 and `npm` >= 9, then
```sh
npm i
```
## Compile
```sh
npx hardhat compile
```
## Test
```sh
npx hardhat test
```

## Deploy
```sh
npx hardhat deploy
```